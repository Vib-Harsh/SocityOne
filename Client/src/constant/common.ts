import type { action_key } from "@/types";

export const actions: Record<action_key, action_key> = {
  add: "add",
  edit: "edit",
  delete: "delete",
  view: "view",
  export: "export",
  import: "import",
};
